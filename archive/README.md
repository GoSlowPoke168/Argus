# Archive

Data files no longer used by the active app or pipeline, kept instead of deleted.

- `windy_massive.geojson` — output of the retired `scripts/legacy/fetch_windy_massive.py` /
  `fetch_windy_dense.py` scrapers (superseded by `scripts/scrapers/global/windy.py`).
- `ca_test.geojson` — unreferenced scratch/test data, origin unclear.
